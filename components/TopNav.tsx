"use client";

// TopNav is now replaced by CerroBalancePill (mounted in layout.tsx).
// This file is kept empty for backward compat — no component rendered.
// The old TopNav with logo is gone; the brand is now in the Sidebar (desktop)
// and the BottomNav handles mobile navigation.

export function TopNav() {
  return null;
}
